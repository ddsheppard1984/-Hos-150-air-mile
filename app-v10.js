(()=>{const KEY='hos150v10';const H=36e5,$=i=>document.getElementById(i);let S=JSON.parse(localStorage.getItem(KEY)||'null')||{status:'OFF DUTY',currentStart:Date.now(),segments:[],events:[],pos:null,base:null,cycleRemaining:null,cycle:60,records:[],docs:[],shiftStart:null,inspect:{pre:{},post:{}}},watch=null,stop=null,map=null,me=null,circle=null,line=null;const save=()=>localStorage.setItem(KEY,JSON.stringify(S)),fmt=x=>{x=Math.max(0,x||0);let m=Math.floor(x/6e4);return String(Math.floor(m/60)).padStart(2,'0')+':'+String(m%60).padStart(2,'0')},active=s=>{let t=0;(S.segments||[]).forEach(x=>{if(x.status===s)t+=x.end-x.start});if(S.status===s&&S.currentStart)t+=Date.now()-S.currentStart;return t},air=(a,b,c,d)=>{let R=3958.761,p=Math.PI/180,x=(c-a)*p,y=(d-b)*p,q=Math.sin(x/2)**2+Math.cos(a*p)*Math.cos(c*p)*Math.sin(y/2)**2;return R*2*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))};function duty(s,src='DRIVER'){if(s===S.status)return;if(S.currentStart)S.segments.push({status:S.status,start:S.currentStart,end:Date.now()});S.status=s;S.currentStart=Date.now();S.events.unshift({time:new Date().toISOString(),status:s,loc:S.pos?`${S.pos.lat.toFixed(5)},${S.pos.lon.toFixed(5)}`:'GPS unavailable',source:src,note:'Duty status change'});save();render()}function show(id,b){document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));$(id)?.classList.add('active');document.querySelectorAll('nav button').forEach(x=>x.classList.remove('sel'));b?.classList.add('sel');if(id==='trip')setTimeout(initMap,100);if(id==='log')drawLog()}document.querySelectorAll('nav [data-tab]').forEach(b=>b.onclick=()=>show(b.dataset.tab,b));document.querySelectorAll('[data-duty]').forEach(b=>b.onclick=()=>duty(b.dataset.duty));$('startShift').onclick=()=>{S.shiftStart=S.shiftStart||Date.now();if(S.status==='OFF DUTY')duty('ON DUTY');save();render()};$('endShift').onclick=()=>{if(S.currentStart)S.segments.push({status:S.status,start:S.currentStart,end:Date.now()});S.status='OFF DUTY';S.currentStart=Date.now();S.shiftStart=null;save();render()};function render(){let d=active('DRIVING'),on=active('ON DUTY'),sl=active('SLEEPER'),off=active('OFF DUTY'),dr=Math.max(0,11*H-d),used=S.shiftStart?Date.now()-S.shiftStart:Math.min(14*H,d+on),sh=Math.max(0,14*H-used),cy=S.cycleRemaining==null?null:Math.max(0,S.cycleRemaining*H-(d+on));[['driveLeft',fmt(dr)],['shiftLeft',fmt(sh)],['driveTot',fmt(d)],['driveTotal',fmt(d)],['onTot',fmt(on)],['onTotal',fmt(on)],['offTot',fmt(off)],['sleepTot',fmt(sl)],['cycleLeft',cy==null?'--':fmt(cy)],['current',S.status]].forEach(a=>$(a[0])&&($(a[0]).textContent=a[1]));if($('b11'))$('b11').style.width=100*d/(11*H)+'%';if($('b14'))$('b14').style.width=100*used/(14*H)+'%';document.querySelectorAll('[data-duty]').forEach(b=>b.classList.toggle('active',b.dataset.duty===S.status));let w=[];if(dr<18e5)w.push('Driving time is under 30 minutes.');if(sh<18e5)w.push('14-hour window is under 30 minutes.');if($('legal')){$('legal').className='status '+(w.length?'warn':'good');$('legal').textContent=w.length?'⚠️ '+w.join(' '):'🟢 Within prototype HOS clocks.'}if($('break')){$('break').className='status '+(d>8*H?'warn':'neutral');$('break').textContent=d>8*H?'⚠️ Break attention required':'No warning'}if(S.base&&S.pos){let m=air(S.base.lat,S.base.lon,S.pos.lat,S.pos.lon);if($('miles'))$('miles').textContent=m.toFixed(1);if($('radius')){$('radius').textContent=m<=150?'🟢 INSIDE 150':'🔴 OUTSIDE 150';$('radius').className='status '+(m<=150?'good':'bad')}if($('elig')){$('elig').className='status '+(m<=150?'good':'bad');$('elig').textContent=m<=150?'🟢 Reporting location • within 150 air miles':'🔴 Reporting location • outside 150 air miles'}}else{if($('miles'))$('miles').textContent='--';if($('radius'))$('radius').textContent='GPS OFF'}renderEvents();renderRecords();renderDocs();renderSettings();drawMap()}function hideGate(){$('gpsGate')&&($('gpsGate').style.display='none')}function gate(msg,bad){if(!$('gpsGate'))return;$('gpsGate').style.display='flex';if($('gpsMsg')){$('gpsMsg').className='status '+(bad?'bad':'warn');$('gpsMsg').textContent=msg}}function gps(){if(!window.isSecureContext)return gate('Open the HTTPS GitHub Pages address for GPS.',true);if(!navigator.geolocation)return gate('This browser has no GPS API. You can continue without GPS.',true);$('gpsMsg').textContent='Requesting location permission…';navigator.geolocation.getCurrentPosition(p=>{S.pos={lat:p.coords.latitude,lon:p.coords.longitude,speed:p.coords.speed||0};hideGate();save();watchGPS();render()},e=>gate(e.code===1?'Permission denied. Enable Location Services in iPhone Settings, then retry.':e.code===2?'Location unavailable. Continue without GPS or retry later.':'GPS timed out. Continue without GPS or retry.',true),{enableHighAccuracy:true,maximumAge:10000,timeout:8000})}function watchGPS(){if(watch!==null)navigator.geolocation.clearWatch(watch);watch=navigator.geolocation.watchPosition(p=>{S.pos={lat:p.coords.latitude,lon:p.coords.longitude,speed:p.coords.speed||0};let mph=Math.max(0,(p.coords.speed||0)*2.236936);if($('speed'))$('speed').textContent=mph.toFixed(0)+' MPH';if(mph>=5){stop=null;if($('motion')){$('motion').className='status good';$('motion').textContent='🚛 MOVING — phone GPS prototype'}if(S.status!=='DRIVING')duty('DRIVING','GPS')}else{if($('motion')){$('motion').className='status blue';$('motion').textContent='⏸️ STOPPED — 5-minute timer'}if(!stop)stop=Date.now();if(S.status==='DRIVING'&&Date.now()-stop>=300000&&S.autoMode!=='off'){if(confirm('Stopped for 5 minutes. Record ON DUTY NOT DRIVING?'))duty('ON DUTY','AUTO-STOP');stop=null}}save();render()},()=>gate('GPS signal was lost. The app remains usable; retry GPS when ready.',true),{enableHighAccuracy:true,maximumAge:3000,timeout:10000})}$('enableGps').onclick=gps;$('gpsHelp').onclick=()=>alert('iPhone: Settings → Privacy & Security → Location Services → ON → your browser → While Using. If opened inside another app, open the GitHub Pages address directly in Safari.');let cb=document.createElement('button');cb.textContent='CONTINUE WITHOUT GPS';cb.className='secondary';cb.onclick=hideGate;$('gpsGate')?.querySelector('.gatebox')?.insertBefore(cb,$('gpsHelp'));async function geo(q){let r=await fetch('https://nominatim.openstreetmap.org/search?format=json&limit=1&q='+encodeURIComponent(q)),a=await r.json();if(!a.length)throw Error();return[+a[0].lon,+a[0].lat,a[0].display_name]}$('saveBase').onclick=async()=>{try{let p=await geo($('base').value.trim());S.base={lon:p[0],lat:p[1],name:p[2]};save();render();alert('Normal reporting location saved.')}catch(e){alert('Location not found.')}};$('gpsBase').onclick=()=>{if(!S.pos)return alert('Enable GPS first.');S.base={lat:S.pos.lat,lon:S.pos.lon,name:'Current GPS location'};save();render()};function initMap(){if(map||!window.L||!$('map'))return;map=L.map('map').setView(S.pos?[S.pos.lat,S.pos.lon]:[39,-96],5);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap contributors'}).addTo(map);drawMap()}function drawMap(){if(!map)return;if(S.pos){if(!me)me=L.marker([S.pos.lat,S.pos.lon]).addTo(map);else me.setLatLng([S.pos.lat,S.pos.lon])}if(S.base){if(!circle)circle=L.circle([S.base.lat,S.base.lon],{radius:241402,fill:false}).addTo(map);else circle.setLatLng([S.base.lat,S.base.lon])}if(S.route&&!line){line=L.geoJSON({type:'LineString',coordinates:S.route}).addTo(map)}}$('planTrip').onclick=async()=>{try{let q=$('destination').value.trim();if(!q)return alert('Enter a destination.');let st=S.pos?[S.pos.lon,S.pos.lat]:(await geo($('start').value||'Portland, ME')).slice(0,2),en=(await geo(q)).slice(0,2),j=await(await fetch(`https://router.project-osrm.org/route/v1/driving/${st[0]},${st[1]};${en[0]},${en[1]}?overview=full&geometries=geojson`)).json();if(!j.routes?.length)throw Error();S.route=j.routes[0].geometry.coordinates;save();initMap();if(line)line.remove();line=L.geoJSON(j.routes[0].geometry).addTo(map);map.fitBounds(line.getBounds());let mi=j.routes[0].distance/1609.344,m=Math.round(j.routes[0].duration/60000);$('tripMiles').textContent=mi.toFixed(0)+' mi';$('eta').textContent=Math.floor(m/60)+'h '+m%60+'m';$('tripSummary').className='status good';$('tripSummary').textContent='Route ready • '+$('tripMiles').textContent+' • '+$('eta').textContent;weather(en[1],en[0],'routeWeather')}catch(e){$('tripSummary').className='status bad';$('tripSummary').textContent='Route unavailable. Try another destination.'}};async function weather(lat,lon,t){try{let j=await(await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current=temperature_2m,wind_speed_10m,weather_code&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max&timezone=auto`)).json(),rain=j.daily.precipitation_probability_max[0]||0;if(t==='routeWeather'){$(t).innerHTML=`Destination: <b>${j.current.temperature_2m}°</b> • Wind ${j.current.wind_speed_10m} • Rain ${rain}%`;$('routeRisk').className='status '+(rain>=60?'warn':'good');$('routeRisk').textContent=rain>=60?'⚠️ Wet-weather signal':'🟢 Lower precipitation signal'}else{$(t).className='status blue';$(t).innerHTML=`Current: <b>${j.current.temperature_2m}°</b> • Wind ${j.current.wind_speed_10m} • Rain ${rain}%`;$('forecast').innerHTML=j.daily.time.slice(0,7).map((d,i)=>`<div class="card"><b>${d}</b><strong>${Math.round(j.daily.temperature_2m_max[i])}°</strong><div class="muted">Low ${Math.round(j.daily.temperature_2m_min[i])}° • Rain ${j.daily.precipitation_probability_max[i]}%</div></div>`).join('')}}catch(e){$(t).textContent='Weather service unavailable.'}}$('getWeather').onclick=async()=>{try{let p=await geo($('weatherPlace').value);weather(p[1],p[0],'weatherNow')}catch(e){$('weatherNow').textContent='Location not found.'}};$('weatherHere').onclick=()=>S.pos?weather(S.pos.lat,S.pos.lon,'weatherNow'):alert('Enable GPS first.');document.querySelectorAll('[data-poi]').forEach(b=>b.onclick=async()=>{if(!S.pos)return alert('Enable GPS first.');let a=b.dataset.poi==='fuel'?'fuel':'parking';try{let u='https://overpass-api.de/api/interpreter?data='+encodeURIComponent(`[out:json];nwr[amenity=${a}](around:30000,${S.pos.lat},${S.pos.lon});out center 10;`),j=await(await fetch(u)).json();$('poi').innerHTML=j.elements.map(x=>`<div class="status neutral"><b>${x.tags?.name||b.dataset.poi}</b><br><span class="muted">Nearby map result</span></div>`).join('')||'No results.'}catch(e){$('poi').textContent='POI service unavailable.'}});function renderEvents(){$('events').innerHTML=(S.events||[]).slice(0,100).map(e=>`<tr><td>${new Date(e.time).toLocaleString()}</td><td>${e.status}</td><td>${e.loc}</td><td>${e.source}</td><td>${e.note}</td></tr>`).join('')}function drawLog(){let g=$('logGrid');if(!g)return;g.innerHTML='';for(let h=0;h<24;h++){let d=document.createElement('div');d.className='hour';d.innerHTML='<div class="lane l0"></div><div class="lane l1"></div><div class="lane l2"></div><div class="lane l3"></div><span>'+h+'</span>';g.appendChild(d)}}function checks(t){let e=$(t);if(e)e.innerHTML=items.map((x,i)=>`<label class="check"><input type="checkbox" data-${t}="${i}">${x}</label>`).join('')}checks('pre');checks('post');document.querySelectorAll('[data-complete]').forEach(b=>b.onclick=()=>{let t=b.dataset.complete;S.inspect[t]={time:new Date().toISOString(),items:[...document.querySelectorAll(`[data-${t}]`)].map(x=>x.checked)};save();$(t+'Msg').className='status good';$(t+'Msg').textContent='Completed '+new Date().toLocaleString()});document.querySelectorAll('[data-clear]').forEach(b=>b.onclick=()=>{let t=b.dataset.clear;S.inspect[t]={};document.querySelectorAll(`[data-${t}]`).forEach(x=>x.checked=false);save();$(t+'Msg').textContent='Not completed.'});$('saveDefect').onclick=()=>{localStorage.setItem('hos150_defect',$('defect').value);$('defectMsg').className='status '+($('defect').value.trim()?'warn':'neutral');$('defectMsg').textContent=$('defect').value.trim()?'Defect saved locally.':'No defect saved.'};$('saveRecord').onclick=()=>{S.records.unshift({date:$('rdate').value,in:$('rin').value,out:$('rout').value,hours:$('rhrs').value,note:$('rnote').value});save();renderRecords()};function renderRecords(){$('records').innerHTML=(S.records||[]).map(r=>`<tr><td>${r.date}</td><td>${r.in}</td><td>${r.out}</td><td>${r.hours}</td><td>${r.note}</td></tr>`).join('')}$('addDoc').onclick=()=>{if(!$('docName').value||!$('docDate').value)return;S.docs.push({name:$('docName').value,date:$('docDate').value,note:$('docNote').value});save();renderDocs()};function renderDocs(){$('docs').innerHTML=(S.docs||[]).map(d=>`<div class="status ${new Date(d.date+'T23:59:59')<new Date()?'bad':'neutral'}">📄 ${d.name} — ${d.date}<br><span class="muted">${d.note||''}</span></div>`).join('')}function renderSettings(){['driver','carrier','dot','unit','vin','trailer'].forEach(k=>$(k)&&($(k).value=S[k]||''));if($('cycleType'))$('cycleType').value=S.cycle||60;if($('cycleStart'))$('cycleStart').value=S.cycleRemaining??'';if($('baseMsg'))$('baseMsg').textContent=S.base?'Saved: '+S.base.name:'Not set.'}$('certify').onclick=()=>{$('cert').className='status good';$('cert').textContent='Prototype log certified locally at '+new Date().toLocaleString()};$('printLog').onclick=()=>window.print();$('export').onclick=()=>{let rows=[['Time','Status','Location','Source','Note'],...S.events.map(e=>[e.time,e.status,e.loc,e.source,e.note])],csv=rows.map(r=>r.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='hos150-events.csv';a.click()};function settings(){['driver','carrier','dot','unit','vin','trailer'].forEach(k=>S[k]=$(k).value);save();render()}$('saveSettings').onclick=settings;$('saveHOS').onclick=()=>{S.cycle=+$('cycleType').value;let c=parseFloat($('cycleStart').value);S.cycleRemaining=Number.isFinite(c)?c:null;save();render()};$('reset').onclick=()=>{if(confirm('Reset this prototype day?')){localStorage.removeItem(KEY);location.reload()}};window.addEventListener('online',()=>{$('net')&&($('net').textContent='ONLINE')});window.addEventListener('offline',()=>{$('net')&&($('net').textContent='OFFLINE READY')});setInterval(render,1000);render();setTimeout(()=>{if(S.pos)watchGPS();else gate('GPS is optional. Enable it for live radius, trip position and motion detection.',false)},300)})();